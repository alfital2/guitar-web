# Perf review "do now" implementation report — 2026-07-02

Branch `wow-backgrounds`, commit `01ea24b` — "perf: teardown contract, worklet destroy, tuner throttle, curve guards, metronome stop".
Scope: items #1, #3, #4, #7 from `docs/code-review-2026-07-01.md` plus the AudioWorklet destroy addition. No "do soon"/"defer" items touched; nothing in the js-rocks-research territory touched; user WIP (`index.html`, `src/chain-ui/*`, `src/bg.js`, `src/fx*`, untracked dirs) untouched and NOT committed (staged files were listed explicitly; never `git add -A`).

## #1 destroy() teardown contract (oscillator/source leak on every chain edit)

**Per-effect `destroy()`** — each of the 9 LFO effects now returns `destroy()` that stops every started source and disconnects every node it created:
- `src/effects/chorus.js:39-46` (osc)
- `src/effects/flanger.js:45-51` (osc)
- `src/effects/phaser.js:61-68` (osc + ConstantSource `lfoOffset` + 6 all-pass stages)
- `src/effects/vibrato.js:32-38` (osc)
- `src/effects/tremolo.js:32-38` (osc)
- `src/effects/rotary.js:76-84` (2 rotor oscs; `rotor()` at :35 now also returns its delay/panner so they're disconnected)
- `src/effects/autopan.js:33-39` (osc)
- `src/effects/ringmod.js:38-44` (carrier osc)
- `src/effects/tape-echo.js:56-62` (flutter osc; also breaks the sat feedback loop)

**Engine aggregation** — `src/engine.js:20` passes `built.destroy` through; `src/engine.js:31-42` adds `engine.destroy()` which per-module calls `m.destroy?.()` inside try/catch (one failure doesn't block the rest) and disconnects each module's input/output. Effects without sources may omit destroy.

**rebuildGraph** — `src/main.js:225-228`: the OLD `engine.destroy?.()` is called (guarded) after the existing output disconnects, before `engine` is reassigned. Runs on add/remove/move/bypass-toggle.

**AudioWorklet addition (abandoned nodes kept processing forever):**
- `src/effects/worklets/neural-amp-processor.js:59-71,131` — handles `{type:'destroy'}`; sets `this.destroyed`; `process()` returns `false` immediately (never touches the wasm again). This kills the worst leak: each abandoned neuralamp node ran full WaveNet inference (~13% of a core) per rebuild.
- `src/effects/worklets/pitchshift-processor.js:20-23,36` — same flag + port handler (it always returned `true`; verified).
- `src/effects/worklets/looper-processor.js:21-23,27` — same (also always returned `true`).
- Effect modules post the message and disconnect: `src/effects/neuralamp.js:118-131` (also nulls `port.onmessage`, idempotent guard), `src/effects/pitchshift.js:22-29`, `src/effects/looper.js:23-30`.

## #3 Tuner throttle + NSDF preallocation

- `src/tuner.js:37-49` — detection gated to ~15 Hz (`DETECT_MS = 66`, `performance.now()` gate inside the rAF loop, mirroring the meter throttle at `src/main.js:530-535`); the rAF keeps ticking so `stop()`'s single cancel handle still works.
- `src/pitch/mpm.js:6-15` — the NSDF `Float32Array(maxLag+1)` is now a module-level scratch reused across calls (reallocated only when maxLag changes). Placed in the detector so BOTH callers (tuner loop and main.js meter loop) stop allocating per detection. Safe: every element is overwritten each call and the buffer never escapes `detectPitchMPM`.

## #4 WaveShaper curve rebuild guards

All follow the `reverb.js:22-28` `lastSize` pattern — cache the source param, rebuild the `Float32Array` curve only when it changed:
- `src/effects/drive.js:36-45` — `lastAmount` guards `makeSoftClipCurve`
- `src/effects/fuzz.js:22-30` — `lastFuzz` guards `makeHardClipCurve`
- `src/effects/octave.js:30-43` — `lastOctave` + `lastFuzz` guard the two curves independently
- `src/effects/gate.js:35-43` — `lastThreshold` guards `makeGateCurve`

## #7 Metronome teardown

`src/metronome.js`:
- `:33-38` `after()` helper tracks every pending `setTimeout` id in a `pendingTimeouts` Set (beat-UI callbacks at :64, armed downbeat at :71); `click()` (:40-50) tracks scheduled oscillators in `liveOscs` (removed + disconnected via `onended`).
- `:95-110` — split: `stopTicking()` (internal grid halt) vs public `stop()` which additionally clears all pending timeouts and stops every scheduled oscillator. A scheduled count-in `onDownbeat` no longer fires after the user hits stop.
- Subtlety handled: the count-in-only path (`:77`) previously called `stop()` right after arming the downbeat timeout — with the new cancelling stop() that would have killed recording start. It now calls `stopTicking()` so the armed downbeat (and the already-scheduled count clicks) survive the internal halt, while a real user `stop()` during the lead window still cancels everything. Covered by a dedicated test.

## Tests (TDD evidence)

New/extended:
- `tests/engine.test.js:47-84` — destroy contract: chorus+phaser chain on the stub ctx; asserts 2 osc starts + 1 ConstantSource start are all stopped, module boundary nodes disconnected, no oscillator edges remain; one throwing module destroy doesn't block the next (tremolo still stopped); destroy-less modules (compressor/eq) don't throw.
- `tests/new-effects.test.js:58-66` — sweep across all 17 expanded effects: any effect that started a source MUST return `destroy()`, and calling it balances stops against starts (osc + const).
- `tests/new-effects.test.js:69-109` — curve guards for fuzz/octave(both curves independently)/gate: apply with unrelated params → same curve object identity; source param change → new object.
- `tests/drive.test.js:35-48` — same identity check for drive's amount curve.
- `tests/neural-amp-processor.test.js:128-145` — after `{type:'destroy'}` `process()` returns `false` (and stays false), both for a model-loaded processor and a passthrough one; all 6 pre-existing worklet tests untouched and green.
- `tests/metronome-teardown.test.js` (new file — metronome.js captures `AC` at import, so a fake `AudioContext` global must be installed before a dynamic import; vitest's per-file module registry keeps `metronome.test.js`'s no-audio path intact) — fake timers: stop() zeroes `vi.getTimerCount()` and stops the ahead-scheduled click; stop() during count-in cancels the armed downbeat; count-in-only internal halt keeps the downbeat alive.
- `tests/chorus.test.js:26-34` — chorus destroy stops the LFO and disconnects.
- `tests/fake-audio-context.js` — stub extended: `oscStops`/`constStops` counters, `nodesByKind` index, `exponentialRampToValueAtTime` on params (needed by metronome's click envelope).

Red→green: the metronome teardown test initially failed (`oscStops` expected 1, got 2) because `click()` itself schedules `osc.stop(t+0.06)`; assertion corrected to expect both the scheduled stop and the teardown stop.

## Verification (both run after all changes, before commit)

- `npx vitest run`: **Test Files 1 failed | 49 passed (50); Tests 2 failed | 289 passed (291)** — the 2 failures are `tests/pedalboard.test.js` fx-art assertions **pre-existing from the user's uncommitted chain-ui WIP**, proven unrelated: pristine HEAD in a temp worktree passes 10/10, and overlaying ONLY the user's `src/chain-ui/{fx-art,pedalboard,amp}.js` WIP reproduces exactly those 2 failures. Baseline before my work was identical (276/278 with the same 2 red). All 276 previously-passing tests still pass; my changes add 13 passing tests (289 total).
- `npm run test:e2e`: **✅ NEURAL E2E PASS** — "Professional amps load, stream live, respond to pedals, switch cleanly, channel selector works" (5 amps, RMS spread 4.68x, 0 uncaught page errors). This exercises the real neural path through preset switches (i.e. rebuildGraph → old-engine destroy → new worklet) post-change.

## Concerns

1. The 2 pedalboard test failures belong to the user's uncommitted chain-ui WIP (`fx-art use href` assertions) — not introduced, not fixed, not my territory.
2. `neuralamp` destroy makes model switching *within one engine instance* unaffected (apply() reposts models as before); a destroyed node is unrecoverable by design — matches the "fresh preset load = fresh node" architecture. The backlog story d529d8a9 (in-place worklet rebuild) will layer on top cleanly.
3. Metronome: during the ≤90 ms REC_LEAD window of a count-in-only arm, the grid has already halted itself; a user `stop()` in that exact window still cancels the downbeat (tested) — behavior is strictly better than before.
